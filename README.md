# Advent_of_code
My attempt at AOC from 2024 onward


In the input folder, the folders are named after the day and if they are example or real input in the format of `{day}_{example/real}.txt`

when the day has multiple parts using different inputs, the folder is named in the format `{day}_{part}_{example/real}.txt`.